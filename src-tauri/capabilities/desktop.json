{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Capabilities for plugins that only exist on desktop",
  "windows": ["main", "preview-*"],
  "platforms": ["macOS", "windows", "linux"],
  "permissions": ["updater:default"]
}
